import { DisableTaskDirective } from './disable-task.directive';

describe('DisableTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableTaskDirective();
    expect(directive).toBeTruthy();
  });
});
